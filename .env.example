# Copy this file to `.env.local` or `.env` and fill the values from your Firebase project settings.
# Vite requires env variables to start with VITE_ to be exposed to the client.

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# Optional: set to 'true' to enable analytics (if configured)
VITE_FIREBASE_ENABLE_ANALYTICS=false
